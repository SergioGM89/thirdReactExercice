import ReactDOM from 'react-dom/client';
import React from 'react';
import Picker from './Picker';

const root = ReactDOM.createRoot(document.querySelector('#picker-container'));
root.render(<Picker/>);